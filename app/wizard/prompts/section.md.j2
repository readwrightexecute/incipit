PROJECT CONTEXT
===============
IDEA:
{{ idea }}

PROJECT TYPE: {{ project_type }} ({{ project_type_hint }})
STAKES: {{ stakes }} ({{ stakes_hint }})
FORM FACTOR: {{ form_factor }}
{% if repo_context %}
EXISTING CODEBASE CONTEXT (summary of {{ repo_url }} — make this section fit the real codebase; reference actual modules/structure and prefer additive, non-breaking changes):
{{ repo_context }}
{% endif %}
CLARIFICATIONS (question → answer):
{% for qa in qas %}
- {{ qa.question }}
  → {{ qa.answer }}
{% endfor %}
{% if prior_sections %}
SECTIONS ALREADY DRAFTED (for consistency — do not repeat their content):
{% for s in prior_sections %}
## {{ s.title }}
{{ s.content }}
{% endfor %}
{% endif %}

TASK
====
{{ instruction }}

Output ONLY the section body (no heading — the heading "{{ title }}" is added separately).
