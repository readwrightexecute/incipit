You are revising an implementation brief to fix specific QA findings. Below is
the current spec, the section ids you may edit, and the findings to address.

THE SPEC
========
{{ spec }}

SECTIONS YOU MAY TARGET (use the exact id)
==========================================
{% for sec in sections %}- {{ sec.id }} — {{ sec.title }}
{% endfor %}
QA FINDINGS TO FIX
==================
{{ findings }}

OUTPUT
======
Output ONLY change blocks, one per fix, separated by a line of exactly three
dashes, in this format and nothing else:

SECTION: <one id from the list above>
CHANGE: <a single imperative instruction telling an editor how to revise that section to resolve the finding>
WHY: <the finding being addressed>
---

Group fixes by section. Skip a finding if it can't be fixed by editing a
section. If there is nothing to fix, output exactly: NO CHANGES.
