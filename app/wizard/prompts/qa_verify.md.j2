You are re-checking a previously QA'd implementation brief (a "mega-prompt")
AFTER fixes were applied. For each earlier finding, decide whether the CURRENT
spec below still exhibits it.

THE CURRENT SPEC
================
{{ spec }}

EARLIER FINDINGS (numbered 1–{{ n }})
=====================================
{{ findings }}

YOUR JOB
========
Judge ONLY these {{ n }} findings against the spec above. Do NOT raise new
issues, reword findings, or split them. For each finding, output exactly one
line, in order:

<number>: RESOLVED
<number>: OPEN — <one short clause naming the spec text that still shows the problem>

Mark RESOLVED only if the spec clearly no longer has the problem (quote/point to
the fix). If you are unsure, mark OPEN. Output ONLY the {{ n }} numbered lines,
nothing else.
