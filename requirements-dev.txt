-r requirements.txt

# Test harness. Tests run fully offline (no network, no real LLM/subprocess).
pytest==9.1.1
# Mocked HTTP transport for httpx (OAuth login/callback + Jira REST tests).
respx==0.22.0
