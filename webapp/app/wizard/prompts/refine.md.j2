PROJECT CONTEXT (summary)
=========================
IDEA:
{{ idea }}

PROJECT TYPE: {{ project_type }} ({{ project_type_hint }})
STAKES: {{ stakes }} ({{ stakes_hint }})
FORM FACTOR: {{ form_factor }}

SECTION TO REVISE: "{{ title }}"
================================
{{ content }}

TASK
====
{{ method_instruction }}

Apply the task to the section above and return the COMPLETE revised section
body. Preserve every existing item, label, and detail that the task does not
explicitly change — do NOT drop, summarize, or replace the rest of the section
with only the changed part. Keep existing identifiers (e.g. FR1, NFR2) and their
numbering. Output ONLY the section body — no heading, no preamble, no commentary.
