-r requirements.txt

# Test harness. Tests run fully offline (no network, no real LLM/subprocess).
pytest==9.1.1
# Mocked HTTP transport for httpx (repo-fetch / OAuth login/callback + Jira REST tests).
respx==0.22.0
# Coverage gate (see pytest.ini addopts / .github/workflows/ci.yml).
pytest-cov==7.1.0
