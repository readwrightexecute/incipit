; Lets skills/ verify itself with a bare `pytest` from this directory, with no
; dependency on the repo root config or the web app. The root suite also picks
; these up (see ../pytest.ini).
[pytest]
testpaths = tests
addopts = -ra
